name: iNAV BRVMHalal (heures de marche)

on:
  schedule:
    - cron: '30 9 * * 1-5'
    - cron: '45 9 * * 1-5'
    - cron: '0,15,30,45 10 * * 1-5'
    - cron: '0,15,30,45 11 * * 1-5'
    - cron: '0,15,30,45 12 * * 1-5'
    - cron: '0,15,30,45 13 * * 1-5'
    - cron: '0,15,30,45 14 * * 1-5'
    - cron: '0,15,30 15 * * 1-5'
  push:
    paths:
      - 'data/intraday_nav.json'
  workflow_dispatch:

jobs:
  inav:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.PAT_TOKEN }}
          fetch-depth: 1

      - name: Verifier heures de marche
        id: check_hours
        run: |
          MARKET_OPEN=$(python3 -c "
          from datetime import datetime, timezone, time as dtime
          now = datetime.now(timezone.utc)
          t = now.time().replace(tzinfo=None)
          print('true' if now.weekday() < 5 and dtime(9,30) <= t <= dtime(15,30) else 'false')
          ")
          echo "market_open=$MARKET_OPEN" >> $GITHUB_OUTPUT

      - name: Attendre 5 minutes
        if: steps.check_hours.outputs.market_open == 'true'
        run: sleep 300

      - name: Setup Python 3.11
        if: steps.check_hours.outputs.market_open == 'true'
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'
          cache-dependency-path: requirements/requirements_cloud.txt

      - name: Installer dependances
        if: steps.check_hours.outputs.market_open == 'true'
        run: pip install -r requirements/requirements_cloud.txt

      - name: Calculer iNAV
        id: calc_inav
        if: steps.check_hours.outputs.market_open == 'true'
        continue-on-error: true
        run: python scripts/scrape_intraday_cloud.py
      - name: Mettre a jour NAV quotidienne
        if: steps.check_hours.outputs.market_open == 'true' && steps.calc_inav.outcome == 'success'
        run: python scripts/calc_nav_cloud.py

      - name: Tracker les echecs iNAV
        if: steps.check_hours.outputs.market_open == 'true'
        env:
          GMAIL_USER: ${{ secrets.GMAIL_USER }}
          GMAIL_APP_PASSWORD: ${{ secrets.GMAIL_APP_PASSWORD }}
        run: python scripts/track_inav_failure.py "${{ steps.calc_inav.outcome }}"

      - name: Commit et push (PAT pour declencher boucle)
        if: steps.check_hours.outputs.market_open == 'true' && steps.calc_inav.outcome == 'success'
        run: |
          git config user.name "cgf-bot"
          git config user.email "bot@cgf-bourse.ci"
          git add data/intraday_nav.json data/nav_intraday_history.json data/launch_state.json data/nav_latest.json data/intraday_failure_count.json
          git diff --staged --quiet || git commit -m "iNAV BRVMHalal: snapshot $(date -u +'%Y-%m-%d %H:%M') UTC"
          git pull --rebase origin main && git push || true

      - name: Commit echec iNAV
        if: steps.check_hours.outputs.market_open == 'true' && steps.calc_inav.outcome != 'success'
        run: |
          git config user.name "cgf-bot"
          git config user.email "bot@cgf-bourse.ci"
          git add data/intraday_failure_count.json
          git diff --staged --quiet || { git pull --rebase origin main && git commit -m "iNAV: echec $(date -u +'%Y-%m-%d %H:%M') UTC" && git push || true; }
